<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.watereagle.mapper.TestMapper">
	<select id="getData1" resultType="com.watereagle.spring.dto.TestDto">
		SELECT no,str_data FROM tbl_test WHERE no = 1
	</select>
	<select id="getData2" resultType="com.watereagle.spring.dto.TestDto">
		SELECT * FROM tbl_test WHERE no = 2
	</select>
	<select id="getData3" resultType="com.watereagle.spring.dto.TestDto">
		SELECT * FROM tbl_test WHERE no = 3
	</select>
	<select id="getData4" resultType="com.watereagle.spring.dto.TestDto">
		SELECT * FROM tbl_test WHERE no = 4
	</select>
</mapper>
