<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.watereagle.academy.springai.aiboard.mapper.AiBoardMapper">

    <delete id="del"> DELETE FROM ai_board WHERE no=#{no} </delete>
    <select id="read"> SELECT * FROM ai_board WHERE no=#{no} </select>
    <select id="getList"> SELECT * FROM ai_board ORDER BY no DESC </select>
    <select id="write"> INSERT INTO ai_board (title, content, id) VALUES (#{title}, #{content},
        #{id}) </select>
    <update id="edit"> UPDATE ai_board SET title=#{title}, content=#{content} WHERE no=#{no} </update>

</mapper>
